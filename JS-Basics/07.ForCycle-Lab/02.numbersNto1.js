function printNumbers(input) {
    let number = Number(input[0]);

    for (let i = number; i > 0; i-- ) {
        console.log(i);
        
    }
}

printNumbers(["3"]);
printNumbers(["5"]);
printNumbers(["6"]);